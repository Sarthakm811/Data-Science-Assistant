# AI Data Science Research Assistant - Configuration Template
# Copy this file to .env and fill in your actual credentials
# IMPORTANT: Never commit .env file to version control!

# ==================== API KEYS ====================

# Google Gemini API Key (Required)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ==================== KAGGLE CREDENTIALS ====================

# Kaggle API Username (Required for dataset search)
# Get from: https://www.kaggle.com/settings/account
KAGGLE_USERNAME=your_kaggle_username

# Kaggle API Key (Required for dataset search)
# Get from: https://www.kaggle.com/settings/account (Download kaggle.json)
KAGGLE_KEY=your_kaggle_key

# ==================== OPTIONAL: BACKEND CONFIG ====================

# Redis Configuration (for backend API)
REDIS_URL=redis://localhost:6379
# Defaults to localhost:6379 if not provided

# Database URL (for backend API)
# Examples:
# DATABASE_URL=sqlite:///./app.db
# DATABASE_URL=postgresql://user:password@localhost/dbname
DATABASE_URL=

# ==================== OPTIONAL: SECURITY ====================

# Secret key for JWT tokens (for backend API)
# Generate a secure random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-in-production

# ==================== OPTIONAL: OBSERVABILITY ====================

# OpenTelemetry endpoint (for monitoring)
OTEL_EXPORTER_OTLP_ENDPOINT=

# ==================== OPTIONAL: FEATURE FLAGS ====================

# Enable debug mode
DEBUG=false

# Enable enterprise EDA features
ENABLE_ENTERPRISE_EDA=true

# ==================== OPTIONAL: LIMITS & THRESHOLDS ====================

# Maximum execution time for code execution (seconds)
MAX_EXECUTION_TIME=300

# Maximum memory for code execution (MB)
MAX_MEMORY_MB=1536

# ==================== NOTES ====================

# 1. API Keys:
#    - Keep this file secure and never commit to version control
#    - Regenerate keys if accidentally exposed
#    - Use environment-specific keys for different deployments
#
# 2. Kaggle Setup:
#    - Visit https://www.kaggle.com/account
#    - Click "Create New Token"
#    - Extract username and key from downloaded kaggle.json
#
# 3. Gemini API:
#    - Visit https://makersuite.google.com/app/apikey
#    - Create a new API key
#    - Keep it private and rotate periodically
#
# 4. Production Deployment:
#    - Use environment variables instead of .env file
#    - Set via Docker environment, Heroku Config Vars, Railway Variables, etc.
#    - Use a secrets manager for sensitive data
#    - Never log API keys
#
# 5. Local Development:
#    - Copy this file to .env
#    - Fill in your credentials
#    - Restart your application
#    - Test that API connections work

# ==================== TROUBLESHOOTING ====================

# Error: "API key invalid"
#   → Check that the key is correctly copied (no spaces, special chars)
#   → Regenerate the key from the provider's console
#
# Error: "Module not found: kaggle"
#   → Install: pip install kaggle
#   → Verify .env has KAGGLE_USERNAME and KAGGLE_KEY
#
# Error: "Connection refused (redis)"
#   → Start Redis: redis-server
#   → Or update REDIS_URL to your Redis instance
#
# Error: "Dataset download failed"
#   → Check internet connection
#   → Verify Kaggle credentials
#   → Check dataset availability
#   → Wait and retry (rate limiting)

# For more help, see:
# - README.md
# - DEBUG_AND_FIXES.md
# - SETUP.md
